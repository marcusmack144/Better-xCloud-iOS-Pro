name: XCloudUltra
options:
  bundleIdPrefix: poshgator
  deploymentTarget:
    iOS: "16.0"
  usesTabs: false
  indentWidth: 4

targets:
  XCloudUltra:
    type: application
    platform: iOS
    deploymentTarget: "16.0"
    sources:
      - path: Sources
    resources:
      - path: Resources
    settings:
      PRODUCT_NAME: XCloudUltra
      PRODUCT_BUNDLE_IDENTIFIER: poshgator.better-xcloud
      INFOPLIST_FILE: Info.plist
      CODE_SIGN_IDENTITY: "-"
      CODE_SIGNING_REQUIRED: "NO"
      CODE_SIGNING_ALLOWED: "NO"
      CODE_SIGN_STYLE: Manual
      MARKETING_VERSION: "1.0"
      CURRENT_PROJECT_VERSION: "1"
      ASSETCATALOG_COMPILER_GLOBAL_ACCENT_COLOR_NAME: AccentColor
      IPHONEOS_DEPLOYMENT_TARGET: "16.0"

schemes:
  XCloudUltra:
    build:
      targets:
        XCloudUltra: all
    run:
      executable: XCloudUltra
    archive: {}
